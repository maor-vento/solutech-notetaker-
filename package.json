{
  "name": "solutech-notetaker",
  "version": "1.0.0",
  "description": "Solutech meeting notetaker — joins Google Meet / Zoom calls via Recall.ai and emails the transcript",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
